import Category from "../../models/CatagoryModel";

// Categories initial Setup
const categories = [
  { name: "playstation" },
  { name: "xbox" },
  { name: "nintendo" },
  { name: "Laptops" },
];

const createCategories = async () => {
  const createCategory = categories.map((cate) => {});
};
